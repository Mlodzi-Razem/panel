{
  "type": "object",
  "properties": {
    "users": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/user"
      }
    }
  },
  "definitions": {
    "user": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "User's login name"
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Groups to add the user to"
        },
        "microk8s": {
          "type": "boolean",
          "description": "Whether to add the user to the microk8s group"
        },
        "prod": {
          "type": "boolean",
          "description": "Whether to create the user on the production servers"
        },
        "public_key": {
          "type": "string",
          "description": "User's public SSH key"
        }
      },
      "required": [
        "name",
        "groups",
        "microk8s",
        "prod",
        "public_key"
      ]
    }
  }
}